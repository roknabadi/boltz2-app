# Boltz-2 Structure Prediction App Dependencies

# Core — boltz handles its own torch/numpy/scipy pins
boltz[cuda]

# Web interface
gradio>=4.0.0

# Visualization
matplotlib>=3.7.0
py3Dmol>=2.0.0

# Optional CUDA kernel optimizations (installed separately in run.sh)
# cuequivariance-ops-torch-cu12